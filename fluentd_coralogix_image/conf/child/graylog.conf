<source>
  @type udp
  port 20001
  bind 0.0.0.0
  body_size_limit 3m
  tag graylog2
  format none
</source>

<match graylog2.**>
    @type coralogix
    privatekey "#{ENV['PRIVATE_KEY']}"
    appname "#{ENV['APP_NAME']}"
    subsystemname "#{ENV['SUB_SYSTEM']}"
</match>