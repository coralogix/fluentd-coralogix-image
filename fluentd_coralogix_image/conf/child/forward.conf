<source>
    @type forward
    port 24224
    bind 0.0.0.0
</source>

<match **>
    @type coralogix
    privatekey "#{ENV['PRIVATE_KEY']}"
    appname "#{ENV['APP_NAME']}"
    subsystemname "#{ENV['SUB_SYSTEM']}"
</match>